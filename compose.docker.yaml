services:
  hearthsignal:
    environment:
      HEARTHSIGNAL_MODE: docker
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    # Docker socket access is root-equivalent. This applies only to this
    # explicit live-monitoring override; demo mode runs as an unprivileged user.
    user: "0:0"
